{
  "name": "filesystem",
  "description": "Guidance for safe file reading, writing, search, and directory navigation.",
  "slash_command": "filesystem",
  "trigger_servers": ["filesystem", "glob", "grep"],
  "version": "1.1.0"
}
